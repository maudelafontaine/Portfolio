import styled from "styled-components";

const Portfolio = () => {
  return (
    <Container>
      <h1>Portfolio</h1>
    </Container>
  );
};

const Container = styled.div``;

export default Portfolio;
