import styled from "styled-components";

const Homepage = () => {
  return (
    <Container>
      <h1>Homepage</h1>
    </Container>
  );
};

const Container = styled.div`
  display: flex;
  height: 800px;
`;

export default Homepage;
