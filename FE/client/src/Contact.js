import styled from "styled-components";

const Contact = () => {
  return (
    <Container>
      <h1>Contact page</h1>
    </Container>
  );
};

const Container = styled.div``;

export default Contact;
